@model IEnumerable<BusinessLogicLayer.ScoreStats>

@{
    ViewBag.Title = "ScoreStats";
}

<h2 class="pageheaders">ScoreStats</h2>

@*<p>
        @Html.ActionLink("Create New", "Create")
    </p>*@
<table class="table">
    <tr>
        <th style="color:#FFFFFF">
            @Html.DisplayNameFor(model => model.UserName)
        </th>
        <th style="color:#FFFFFF">
            @Html.DisplayNameFor(model => model.Count)
        </th>
        <th style="color:#FFFFFF">
            @Html.DisplayNameFor(model => model.AverageScore)
        </th>
        <th style="color:#FFFFFF">
            @Html.DisplayNameFor(model => model.HighScore)
        </th>
        <th style="color:#FFFFFF">
            @Html.DisplayNameFor(model => model.LowestScore)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td style="color:#8BD0EE">
                @Html.DisplayFor(modelItem => item.UserName)
            </td>
            <td style="color:#8BD0EE">
                @Html.DisplayFor(modelItem => item.Count)
            </td>
            <td style="color:#8BD0EE">
                @Html.DisplayFor(modelItem => item.AverageScore)
            </td>
            <td style="color:#8BD0EE">
                @Html.DisplayFor(modelItem => item.HighScore)
            </td>
            <td style="color:#8BD0EE">
                @Html.DisplayFor(modelItem => item.LowestScore)
            </td>
            @*<td>
                    @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                    @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                    @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
                </td>*@
        </tr>
    }

</table>
